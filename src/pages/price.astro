---
import Page from "@/components/Page.astro";
import PricePlans from "@/components/PricePlans.astro";
import { seatPrice } from "@/site-config";

const description = `TeleCrypt early bird — chats and storage for ${seatPrice}.`;
---

<Page title="Price" description={description}>
	<PricePlans />
</Page>
