---
import Page from "@/components/Page.astro";
import { loadPage } from "@/utils/content-page";

const { Content, meta } = await loadPage(
	"technology",
	"How the transport works, and why it's secure.",
);
---

<Page title={meta.title} description={meta.description} label="Technology">
	<Content />
</Page>
